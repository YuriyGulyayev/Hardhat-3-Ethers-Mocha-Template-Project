- The currently installed `forge-std` version is outdated. I preserved the version Hardhat init wizard has installed. In fact, the wizard specified version `#v1.9.4`, which I changed slightly.\
Comment-202603185 relates.\
todo-2 To be revisited.

- ESLint requires the `jiti` NPM package, so I have included it in `package.json`.\
todo-2 But try to remove it when Node.JS fully supports TypeScript.
